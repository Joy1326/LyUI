export default {
    directives: {
        draggable: {
            // 指令的定义
            inserted(el, bind) {
                
            },
            unbind(el, bind) {
                
            }
        }
    }
}